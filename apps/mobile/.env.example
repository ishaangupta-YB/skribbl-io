# Backend WebSocket/REST base URL used by the client.
# Phase 1: local protocol mock (run `pnpm mock` from the repo root).
# Phase 2: point this at `wrangler dev` or the deployed Worker.
EXPO_PUBLIC_WS_URL=ws://localhost:8787
