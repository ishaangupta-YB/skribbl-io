# Backend WebSocket/REST base URL used by the client.
# Phase 2: point this at `wrangler dev` (pnpm --filter @skribbl/api dev) or the deployed Worker.
# The REST helpers in lib/api.ts derive the HTTP base from this by swapping ws→http / wss→https.
EXPO_PUBLIC_WS_URL=ws://localhost:8787
