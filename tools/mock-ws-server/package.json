{
  "name": "@skribbl/mock-ws-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local WebSocket server implementing the @skribbl/shared contract so the Expo client can be developed before the Cloudflare backend is ready.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@skribbl/shared": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
